import Content from "./readme.mdx";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Firezone Docs • Use Cases: Nat Gateway",
  description: "Use Cases: Nat Gateway",
};

export default function Page() {
  return <Content />;
}
