<section class="section is-main-section">
  <div class="block">
    <%= live_patch("<- Back to tunnels", to: Routes.tunnel_unprivileged_index_path(@socket, :index)) %>
  </div>

  <%= render FzHttpWeb.SharedView, "flash.html", assigns %>

  <h4 class="title is-4">Tunnel Details</h4>

  <%= render(FzHttpWeb.SharedView, "tunnel_details.html", assigns) %>
</section>

<section class="section is-main-section">
  <h4 class="title is-4">
    Danger Zone
  </h4>

  <button class="button is-danger"
    phx-click="delete_tunnel"
    phx-value-tunnel_id={@tunnel.id}
    data-confirm="Are you sure? This will immediately disconnect this tunnel and remove all associated data.">
    <span class="icon is-small">
      <i class="fas fa-trash"></i>
    </span>
    <span>Delete Tunnel <%= @tunnel.name %></span>
  </button>
</section>
