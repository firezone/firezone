[package]
name = "tidy-dependabot-pr"
version = "0.1.0"
edition = { workspace = true }
license = { workspace = true }
publish = false

# Zero dependencies on purpose: `cargo run` compiles it in well under a second.

[lints]
workspace = true
