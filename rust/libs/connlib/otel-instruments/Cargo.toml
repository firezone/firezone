[package]
name = "otel-instruments"
version = "0.1.0"
edition = { workspace = true }
license = { workspace = true }

[dependencies]
opentelemetry = { workspace = true, features = ["metrics"] }
tokio = { workspace = true, features = ["time", "sync"] }

[lints]
workspace = true
