[package]
name = "x509-identity-ffi"
version = "0.1.0"
edition = { workspace = true }
license = { workspace = true }

[lib]
name = "x509identity"
crate-type = ["lib", "cdylib", "staticlib"]

[dependencies]
uniffi = { workspace = true }
x509-identity = { workspace = true }

[lints]
workspace = true
