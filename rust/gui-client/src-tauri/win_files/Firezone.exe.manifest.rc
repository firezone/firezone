// Embeds Firezone.exe.manifest as the binary's RT_MANIFEST
// resource (id 1, type 24). Compiled via `embed_resource` from
// build.rs and linked into `firezone-gui-client` only.
1 24 "Firezone.exe.manifest"
